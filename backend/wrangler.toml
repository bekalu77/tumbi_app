# wrangler.toml
name = "tumbi-backend"
main = "src/worker.ts"
compatibility_date = "2024-04-05"

# Bind your R2 bucket. Secrets are managed separately.
[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "tumbiapp"
